server:
  port: 8888
spring:
  application:
    name: config-server
#  profiles:
#    active: native
  cloud:
    config:
      server:
#        native:
#          search-locations: classpath:/config
        git:
          uri: https://github.com/kienpham2k1/clinic-system-config-service.git
          default-label: main
          timeout: 5
          clone-on-start: true
          force-pull: true